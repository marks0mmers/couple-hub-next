import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <main>
      <Head>
        <title>Couple Planner</title>
      </Head>
      Home Page
    </main>
  );
};

export default Home;
